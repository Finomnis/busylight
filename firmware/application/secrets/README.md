The signing key here is obviously not secret because it's checked into the git repository.

Its purpose is to prevent accidentally sending incorrect firmware to the device, and to demonstrate how
firmware signing would work.
